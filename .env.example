# Database
DATABASE_URL="file:./data/stt.db"

# Encryption for API keys (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY="your-32-byte-hex-string-here"

# Environment
NODE_ENV="development"

# Optional: Provider API Keys (can also be configured via Settings UI)
# OPENAI_API_KEY="sk-..."
# DEEPGRAM_API_KEY="..."
# VATIS_TECH_API_KEY="..."
